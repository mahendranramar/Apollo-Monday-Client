import { useEffect } from "react";
export function useWarnDeprecated({ component, condition = true, message }) {
    useEffect(() => {
        warnDeprecated({ component, condition, message });
        // eslint-disable-next-line react-hooks/exhaustive-deps
    }, []);
}
export function warnDeprecated({ component, condition, message }) {
    if (condition && process.env.NODE_ENV !== "production") {
        const warningMessage = `[@vibe/core] ${component}: ${message}`;
        console.warn(warningMessage);
    }
}
//# sourceMappingURL=warn-deprecated.js.map