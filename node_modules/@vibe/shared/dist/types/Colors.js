import { contentColors } from "../utils/colors-vars-map";
const MapStateSelectedColor = {
    positive: "--positive-color-selected",
    negative: "--negative-color-selected",
    primary: "--primary-selected-color",
    warning: "--warning-color-selected"
};
const MapStateSelectedHoverColor = {
    positive: "--positive-color-selected-hover",
    negative: "--negative-color-selected-hover",
    primary: "--primary-selected-hover-color"
};
export function getElementColor(colorValue, isSelectedPalette = false, isSelectedHoverPalette = false) {
    if (contentColors.includes(colorValue)) {
        return `var(--color-${colorValue}${isSelectedPalette ? "-selected" : ""})`;
    }
    if (Object.keys(MapStateSelectedHoverColor).includes(colorValue) &&
        isSelectedHoverPalette) {
        return `var(${MapStateSelectedHoverColor[colorValue]})`;
    }
    if (Object.keys(MapStateSelectedColor).includes(colorValue) && isSelectedPalette) {
        return `var(${MapStateSelectedColor[colorValue]})`;
    }
    return colorValue;
}
//# sourceMappingURL=Colors.js.map