export const getTestId = (elementType, id) => {
    const formattedId = (id !== null && id !== void 0 ? id : "").toString();
    return `${elementType}${formattedId && `_${formattedId}`}`;
};
//# sourceMappingURL=test-ids-utils.js.map