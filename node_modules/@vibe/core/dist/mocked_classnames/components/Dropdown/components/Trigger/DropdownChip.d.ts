import React from "react";
import { type BaseItemData } from "../../../BaseItem";
interface DropdownChipProps<Item extends BaseItemData<Record<string, unknown>>> {
    item: Item;
    onDelete: () => void;
    disabled?: boolean;
    readOnly?: boolean;
    className?: string;
}
declare const DropdownChip: <Item extends BaseItemData<Record<string, unknown>>>({ item, onDelete, disabled, readOnly, className }: DropdownChipProps<Item>) => React.JSX.Element;
export default DropdownChip;
