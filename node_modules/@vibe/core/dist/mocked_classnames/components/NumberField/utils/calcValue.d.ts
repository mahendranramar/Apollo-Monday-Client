import { type NumberFieldProps } from "../NumberField.types";
type CalculateSteppedValueProps = Pick<NumberFieldProps, "value" | "step" | "min" | "max" | "allowOutOfBounds"> & {
    direction: number;
};
export declare const calculateSteppedValue: ({ value, direction, step, min, max, allowOutOfBounds }: CalculateSteppedValueProps) => number;
export {};
