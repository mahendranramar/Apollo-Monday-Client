import { type MutableRefObject } from "react";
import { type ElementContent } from "@vibe/shared";
import { type TooltipProps } from "@vibe/tooltip";
export declare function useEllipsisClass(ellipsis: boolean, maxLines?: number): {
    class: string;
    style: Record<string, string>;
};
export declare function useTooltipProps(ref: MutableRefObject<HTMLElement>, withoutTooltip: boolean, ellipsis: boolean, tooltipProps: Partial<TooltipProps>, children: ElementContent, ignoreHeightOverflow: boolean, overflowTolerance: number): {};
